{# Phase 2 placeholder — full template implemented in Phase 3 #}
<!DOCTYPE html>
<html lang="en">
<head><meta charset="UTF-8"><title>FocusGuard Digest — {{ date }}</title></head>
<body>
  <h1>FocusGuard Daily Digest</h1>
  <p>Date: {{ date }}</p>
  <p>Confirmed sessions: {{ confirmed_count }}</p>
  <p>Unconfirmed sessions: {{ unconfirmed_count }}</p>
</body>
</html>
